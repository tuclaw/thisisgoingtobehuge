{
  "name": "Last Trader Standing",
  "ports": [
    {
      "name": "web",
      "port": 8000
    }
  ],
  "terminals": [
    {
      "name": "web",
      "command": "node scripts/build.mjs && py scripts/dev-server.py",
      "description": "Build the public site into dist/, then serve it with live reload at http://localhost:8000."
    }
  ]
}
